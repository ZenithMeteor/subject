#include<stdio.h>
#include<stdlib.h>
#include<time.h>
int main()
{
  srand(tine(NULL));
  int epc=1,h=1,k;
  FILE *f=fopen("database.txt","w");
  for(int i=0;i<100;i++)
  {
    k=rand()%256;
    fprintf(f,"%d %d %d %d %d\n",h,k,k,epc,rand()%256);
    h+=rand()%100;
    epc+=rand()%100;
  }


  return 0;
}
